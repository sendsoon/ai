---
description: 目录职责与协议分层约束，架构相关任务时应参考
alwaysApply: true
---

# 目录职责

- `mcp/`（TypeScript + Python 双实现）：只处理 MCP 协议适配（tool/resource/prompt 注册、schema 定义、transport 配置）
- `skills/`：只处理 Agent Skills 规范（`SKILL.md` + 触发词 + 示例），纯 Markdown，不含可执行代码
- `core/`：协议无关的共享 client 逻辑（调用私有 API 的 HTTP client、错误码映射），Python 和 TypeScript 各自维护一份，接口保持一致，但不共享代码运行时
- `specs/`：Spec-Driven Development 产出的 spec/plan/tasks 文档

# 语言选型原则

- 需要被 Claude Code / Claude Desktop / Cursor 等生态直接 `pip install` 或 `uvx` 拉起的 MCP server → **Python**（生态目前对 stdio 本地 server 支持最成熟）
- 需要发布到 npm、被 Node/TS 项目直接 import 使用，或部署为远程 Streamable HTTP server → **TypeScript**
- 两种实现都必须暴露完全一致的 tool 名称、输入输出 schema 语义，避免用户在不同语言生态间切换时遇到行为差异

# 协议演进原则

未来出现新协议（MCP 之外）→ 新增目录（如 `future-protocol-x/`），不新增仓库，除非满足仓库拆分信号清单（外部真实 PR、发布节奏严重不一致、技术栈完全不重叠）。
