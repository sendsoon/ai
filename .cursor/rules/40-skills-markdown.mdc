---
description: Agent Skills（SKILL.md）编写规范
globs: ["skills/**/*.md"]
alwaysApply: false
---

# SKILL.md 规范

- 遵循开放的 Agent Skills specification（agentskills.io），YAML frontmatter 必须包含 `name` 和 `description`
- `description` 是触发匹配的关键，要清楚写明"什么情况下应该用这个 skill"，包含具体触发词/使用场景，而不是笼统的功能介绍
- 正文只写"怎么调用、参数是什么、常见错误怎么处理"，**不写后端怎么实现**（验证算法、发信策略等私有逻辑一律不出现）
- 每个 SKILL 目录下放 `examples/`，包含 2-3 个端到端调用示例，不含真实凭证（用占位符如 `YOUR_API_KEY`）
- 一个 SKILL.md 对应一个内聚的能力单元（例如 `email-outreach`），不要把多个不相关能力塞进一个 SKILL

# 跨平台兼容

- 文件放在中立目录 `.agent/skills/<skill-name>/SKILL.md`（多数通用安装工具默认读取路径）
- 如果需要兼容 Claude Code 专用路径，在 README 里写明手动 symlink 到 `.claude/skills/` 的方法，不要在仓库里同时维护两份内容重复的文件
