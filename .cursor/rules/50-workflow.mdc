---
description: 涉及新功能、架构调整、技术选型时应遵循的工作流——先出方案，确认后再写代码
globs: ["mcp/**/*", "core/**/*", "skills/**/*"]
alwaysApply: false
---

# 先方案，后实施

当任务涉及以下任一情况时，**先输出技术方案，不要直接开始写代码**，等待人工回复确认或选择后再继续：

- 新增一个 MCP tool 或 SKILL
- 调整 `core/` 共享 client 的接口设计
- 引入新的第三方依赖
- 涉及协议版本升级（例如未来评估从 v1 迁移到 v2 SDK）

# 方案输出格式

```
## 需求理解
[一两句话复述要做什么]

## 候选方案
1. 方案 A：[做法] —— 优点 / 缺点
2. 方案 B：[做法] —— 优点 / 缺点（如果只有一个合理方案，直接说明理由，不用凑数）

## 推荐
推荐方案 [A/B]，原因：[简述]

## 涉及改动
- 新增/修改文件：[列表]
- 是否需要新依赖：[是/否，具体是什么]
```

方案确认后再开始实施。

# 例外

以下情况可以跳过方案确认、直接修改：
- 明显的 bug 修复（行为不符合预期）
- 拼写错误、注释、格式化
- 已经在当前对话中明确讨论并确认过的改动
